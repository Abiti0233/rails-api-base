import React from "react";
import Styles from "../../styles/components/footer.module.scss"

export default function Footer() {
  return (
    <footer className={Styles.footer}>
      <div className={Styles.p}>
        user name's portfolio app
      </div>
    </footer>
  );
}
